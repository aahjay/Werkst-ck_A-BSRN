import time

def stat():
    values = []
    while True:
        value = ()
         if value:
                value = int(value)
                values.append(value)
                mean = sum(values) / len(values)
                total = sum(values)

    
